%p#notice= notice
%h1 掲示板テスト
%table{:align => "center", :border => "3", :style => "table-layout: fixed;", :width => "100%"}
  %thead
    %tr
      %th ID
      %th Name
      %th Comment
      %th{:width => "300"} Image
      %th{:colspan => "3"}  
      / <th colspan="6"> </th>
  %tbody
    - @posts.each do |post|
      %tr
        %td= post.id
        %td= post.name
        %td= post.comment
        %td{:width => "300"}= image_tag post.image_url(:thumb)
        %td= link_to 'Show', post
        %td= link_to 'Edit', edit_post_path(post)
        %td= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Post', new_post_path
%br/
= paginate @posts
